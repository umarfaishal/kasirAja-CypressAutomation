# Cypress Web Automation Project

This is a Cypress project to automation test using kasirAja website. 

## Install & Run

```bash
npm install
npx cypress open
```

Or run headless:

```bash
npx cypress run
```

> Make sure Node.js and npm are installed.
